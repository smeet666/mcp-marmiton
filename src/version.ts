/**
 * Kept in sync with package.json and server.json. The User-Agent embeds it, so it
 * must not drift; the release checklist bumps all three together.
 */
export const PKG_VERSION = "2.0.0";
export const REPO_URL = "https://github.com/smeet666/mcp-marmiton";
